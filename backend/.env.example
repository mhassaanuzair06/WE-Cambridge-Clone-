# Backend environment variables example
# Set a strong secret for admin API operations (POST/PUT/DELETE).
# If ADMIN_KEY is not set, admin routes are allowed (development-friendly).
ADMIN_KEY=change_me_to_secure_value
PORT=3001
